@* <div class="sidebar bg-dark text-white vh-100 d-flex flex-column p-3">
    <!-- Logo -->
    <div class="sidebar-header mb-4">
        <h4 class="text-white">MyApp</h4>
    </div>

    <!-- Navigation Links -->
    <nav class="nav flex-column">
        <NavLink class="nav-link text-white" activeClass="active" href="/">
            <i class="bi bi-speedometer2 me-2"></i> Dashboard
        </NavLink>
        <NavLink class="nav-link text-white" activeClass="active" href="/category">
            <i class="bi bi-tags me-2"></i> Categories
        </NavLink>
        <NavLink class="nav-link text-white" activeClass="active" href="/orders">
            <i class="bi bi-basket2 me-2"></i> Orders
        </NavLink>
        <NavLink class="nav-link text-white" activeClass="active" href="/reports">
            <i class="bi bi-bar-chart me-2"></i> Reports
        </NavLink>
    </nav>

    <div class="mt-auto">
        <NavLink class="nav-link text-white" activeClass="active" href="/settings">
            <i class="bi bi-gear me-2"></i> Settings
        </NavLink>
        <NavLink class="nav-link text-white" activeClass="active" href="/logout">
            <i class="bi bi-box-arrow-right me-2"></i> Logout
        </NavLink>
    </div>
</div>

<style>
    .sidebar {
        width: 250px;
    }
    .sidebar .nav-link {
        padding: 10px;
        border-radius: 4px;
    }
    .sidebar .nav-link.active {
        background-color: rgba(255, 255, 255, 0.1);
    }
</style> *@
